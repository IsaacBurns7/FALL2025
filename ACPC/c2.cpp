#include <iostream>
#include <algorithm>
#include <vector>
#include <utility>
#include <unordered_map>
#include <stack>
#include <array>
#include <bitset>

using std::cout, std::cin, std::max, std::vector, std::pair, std::unordered_map, std::stack, std::array, std::bitset;

int main(){
    int n;
    int m;
    int C;
    cin >> n;
    cin >> m;
    cin >> C;
    
    int p;
    int w;
    
    const size_t bits = 1e5;

    vector<bitset<bits> > filled(n, bitset<bits>());
    

}