#include <iostream>
#include <vector>
#include <stack>
#include <utility>
#include <queue>

/*
push all the coins into the first row, and then have them fall 
yeah no i have no idea
*/

using std::cout, std::cin, std::vector, std::stack, std::pair;

int main(){
    int n;
    int m;
    int c;
    cin >> n >> m >> c;

    vector<stack<pair<int, int> > > fieldRanges(n, stack<pair<int, int> >());
    for(int i = 0;i<c;i++){
        
    }
}